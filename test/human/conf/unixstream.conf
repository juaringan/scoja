# fromUnixStream (org.scoja.server.target.SelectedUnixStreamSource) test

source1 = fromUnixStream(file = "/tmp/scoja/sockets/us1", threads = 2)
target1 = toFile(name = "/tmp/scoja/unix/stream.log", flush = yes)
source1 >> target1
