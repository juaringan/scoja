# fromUnixDatagram (org.scoja.server.target.UnixDatagramSource) test

source1 = fromUnixDatagram(file = "/tmp/scoja/sockets/ud1", threads = 2)
target1 = toFile(name = "/tmp/scoja/unix/dgram.log", flush = yes)
source1 >> target1
