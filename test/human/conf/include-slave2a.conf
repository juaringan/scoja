#
# To test "include" function.
# This file should not be given directly to Scoja.
# Use include-master.conf, that includes this one.

target1 = toFile(
    name = "/tmp/scoja/include/a.log",
    flush = yes)
